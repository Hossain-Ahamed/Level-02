import { Button } from '../components/UI/Button';

const Home = () => {
    return (
        <div>
            <h1>Home</h1>
            <Button className="btn bg-green-500" outline={true}></Button>
        </div>
    );
};

export default Home;